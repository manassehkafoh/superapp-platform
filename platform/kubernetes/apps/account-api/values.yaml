# Helm values for account-api — auto-generated by deploy pipeline
replicaCount: 2
image:
  repository: acrsuperapp.azurecr.io/account-api
  pullPolicy: IfNotPresent
  tag: ""
service: { type: ClusterIP, port: 8080 }
resources:
  requests: { memory: "128Mi", cpu: "100m" }
  limits: { memory: "256Mi", cpu: "500m" }
autoscaling: { enabled: true, minReplicas: 2, maxReplicas: 8, targetCPUUtilizationPercentage: 70 }
podSecurityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000, seccompProfile: { type: RuntimeDefault } }
containerSecurityContext: { allowPrivilegeEscalation: false, readOnlyRootFilesystem: true, capabilities: { drop: ["ALL"] } }
livenessProbe: { httpGet: { path: /health/live, port: 8080 }, initialDelaySeconds: 15 }
readinessProbe: { httpGet: { path: /health/ready, port: 8080 }, initialDelaySeconds: 10 }
podDisruptionBudget: { enabled: true, minAvailable: 1 }
